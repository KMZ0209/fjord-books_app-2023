<p style="color: green"><%= notice %></p>
<div class="show-item">
<%= render @book %>
</div>
<nav class="page-nav">
  <%= link_to t('views.button.edit'), edit_book_path(@book) %> |
  <%= link_to t('views.button.show'), books_path %>
  <%= button_to t('views.button.destroy'), @book, method: :delete %>
</nav>
