<p style="color: green"><%= notice %></p>
<div class="show-item">
<%= render @book %>
</div>
<nav class="page-nav">
  <%= link_to t('activerecord.button.edit'), edit_book_path(@book) %> |
  <%= link_to t('activerecord.button.show'), books_path %>
  <%= button_to t('activerecord.button.destroy'), @book, method: :delete %>
</nav>
